stages:
  - release-npm

publish:
  stage: release-npm
  script:
    - npm config set -- '//registry.npmjs.org/:_authToken' "${NPM_TOKEN}"
    - npm profile get
  #  - npm publish
  only:
    - web
    - master
  when: 
      manual